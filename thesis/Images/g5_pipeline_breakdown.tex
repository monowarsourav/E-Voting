% =====================================================================
%  G5 - Per-phase pipeline time breakdown (70.5 ms total)
%  --------------------------------------------------------------------
%  Backs claim: SA^2 split (48.8%) + ring sign (37.3%) = 86.1% of pipeline
%  Source: chapter4.tex Table tab:pipeline
% =====================================================================
\begin{tikzpicture}
\begin{axis}[
    width=0.85\textwidth,
    height=7.5cm,
    xbar,
    xmin=0, xmax=40,
    xlabel={Time (ms)},
    symbolic y coords={
        Pedersen Commit,
        ZKP Binary Prove,
        SMDC Generate,
        Paillier Encrypt,
        Ring Sign,
        SA2 Split
    },
    ytick=data,
    nodes near coords,
    nodes near coords align={horizontal},
    nodes near coords style={font=\sffamily\scriptsize, /pgf/number format/.cd, fixed, precision=2},
    bar width=14pt,
    grid=major,
    grid style={dashed, gray!30},
    label style={font=\sffamily\small},
    tick label style={font=\sffamily\scriptsize},
    enlarge y limits=0.12,
    every axis plot/.append style={fill=gray!25, draw=black, thick},
]
\addplot+[xbar] coordinates {
    (0.08,Pedersen Commit)
    (0.24,ZKP Binary Prove)
    (1.73,SMDC Generate)
    (8.69,Paillier Encrypt)
    (27.06,Ring Sign)
    (35.34,SA2 Split)
};
\end{axis}
\end{tikzpicture}
